# language setting
language: ruby

# version numbers
rvm:
  - 1.9.3
notifications:
     email:
         recipients:
             - shivam.bajpai@stackexpress.com

after_install:
 - rake db:migrate
