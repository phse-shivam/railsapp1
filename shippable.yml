# language setting
language: ruby

# version numbers
rvm:
  - 1.9.3
notifications:
     email:
         recipients:
             - shivam.bajpai@stackexpress.com

before_install:
 - rake db:migrate
